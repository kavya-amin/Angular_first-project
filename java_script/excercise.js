function area(r){
    const pi=3.14;
    return pi*r*r;
}
function help(){
    document.write("Enter a number in input field to perform operations like calculating area of circle and generating random number within input range");
}